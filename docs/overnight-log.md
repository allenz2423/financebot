# Overnight implementation log

## Current stopping summary

- **Roadmap status:** Step-by-step Steps 0–6 passed. Roadmap items 1–9 and 11–13 passed; Item 15 passed in `1099ee6`; Items 10 and 14 are actively in progress, with no gate claimed for either. `main` was not changed.
- **Item 10 WIP (`2e31ff0`, `8ef6874`):** Added owner-configurable lesson TTL (0/no expiry by default), expiry-aware and type-scoped epistemic memory retrieval, owner/task dedupe, and verified-success-only lesson reflection. The model receives only generated choice IDs and registered tool labels, then can select a fixed receipt-bound procedure template; it cannot author stored text, facts, decisions, or permissions. Top-level tasks now record the exact origin turn and expose an owner/session-scoped user-message source lookup that fails closed for legacy tasks and redacted content. This is not Item 10 completion: exact-quote FACT/DECISION extraction/persistence and verified web-source resolution still need implementation. Remote reflection uses the configured top-level provider; configured model-candidate retries at that provider/endpoint (e.g. OpenRouter) are not cross-provider fallback.
- **Item 14 WIP (`ae29641`):** Added a host-owned task-environment lease/profile/operation contract and fake adapter only. It binds owner+task, uses fixed no-network/no-credential profiles, streams through an output cap, and tests timeout/cancellation/orphan cleanup. Fresh adversarial review found no blockers after the lazy-stream bound test was added. There is still no real process/container, privileged manager, browser broker, egress proxy, or task VM; this is not an Item 14 gate. Deep Muse research and earlier host hardening are documented below and in `docs/muse-secure-environment-research.md`.
- **Full-suite verification:** latest isolated full suite: 1,295 passed, 1 skipped, 9 failed, 15 errors. The 9 failures are known clean-seed/fixture expectations: emergency-fund health, Discord budget dashboard DB-path patching, lifestyle creep, next-best-dollar seed data, recurring leakage, reconciliation seed data, rewards account seed data, and World Model baseline health/search. The 15 errors are legacy `tests/test_world_model.py` setup/actions failing with `sqlite3.OperationalError: database is locked`. Combined focused suite for Item 10, Item 14 contract, source-turn provenance, and task controller: 108 passed. Tests used a fresh temporary working directory/database, disabled `.env`, unset external credentials, a dummy Discord token, and Plaid sandbox/autosync disabled; no live finance DB or external accounts were used.
- **Latest pushed commits:** `ae29641 [wip] Define task-owned environment contract and fake adapter`; `8ef6874 [wip] Bind reflection sources to exact origin turn`; `2e31ff0 [wip] Add verified task lesson reflection with owner TTL`; all on `agentic-roadmap/full-run`. The user's uncommitted `docs/overnight-instructions.md`, `docs/delilah-specs.md`, `sandbox/`, and `scratch/` changes remain untouched.
- **Human decisions recorded:** affirmative choice replies are input-only; approvals require human grant confirmation; reconciliation notices use migration 008's atomic single-claim index; scheduler defaults remain one worker, one active task per owner, eight queued per lane; children inherit strict-subset tools, cannot delegate recursively, parents yield while awaiting children, and cancellation cascades. New decisions: exactly one bot process per task DB (distributed leases disabled); Item 10 TTL is owner-controlled and defaults to no expiry for this operator; remote reflection via configured provider is allowed without silent fallback; user-authored statements and actually-used web sources may support facts with provenance, while model inference may not; lessons are evidence-linked reusable procedures, not personal facts or permissions; initial reflection is limited to verified successful tasks.
- **Human decisions needed:** none newly blocking the next Item 10 or Item 14 safe implementation slices. Distributed multi-process/multi-host execution remains out of scope by owner decision.
- **Deferred suggestions / constraints:** Item 5 needs an LLM-loop integration test from semantic search through schema exposure and dispatch-time grant rejection; runtime availability stays unknown unless probed. Item 10 still needs an outbound-request/provider-routing integration test, an atomic claim to prevent duplicate concurrent reflection calls, exact-source user/web FACT and user-only DECISION handling, and durable web-source receipt/call binding. Item 11 still wants unknown-receipt stop-path and dispatcher persistence-failure tests, more typed freshness/arithmetic adapters, and semantic artifact validators beyond JSON/CSV structure; optional model review stays off. Item 15 wants a dispatch regression for malformed arguments/scope denial after an eligible miss and consolidation of duplicated control-tool inventories; proposals remain draft-only and not persisted/deduplicated. Step 6 review suggestions (provider-round latency, queued-ID privacy behavior, and combined multi-worker authorized-receipt integration) are below. Item 14 still needs a real narrowly privileged manager/runtime, lifecycle persistence and cleanup-error tests, a deployment threat review, task-scoped browser broker/profile, SSRF-safe egress, executor-only secrets, and optional microVM backend. The fake adapter does not prove a future adapter streams incrementally; require a lazy-stream bound test for each real backend. Distributed leases remain disabled by owner decision. For Item 13, preferred-model/notification settings remain inspection-only; unscoped user-anchor lookup fails closed; `!inspectmemory` notes its 500-claim cap.
- **Deferred Item 6 suggestions:** add an integration test that forces cancellation's terminal receipt persistence to fail and verifies the user is told the outcome may have taken effect; add a dispatcher integration test proving a denied cancellation/monitor/delegation call invokes no tool body and creates no grant or receipt. Also desirable: exercise actual LLM-loop early exits against the final evidence guard and broaden indirect/compound mutation-phrase tests. These remain fail-closed in code, but current coverage is primarily policy/helper-level.
- **Deferred Item 12 suggestions:** add a genuinely simultaneous multi-connection phase-transition-versus-steer stress test (current CAS, second-connection snapshot, and deterministic interposed dispatch-race tests cover the safety boundary; distributed DB use is disabled); consider a stronger redaction policy for bounded receipt summaries and steering corrections. `inspect_task(trace)` deliberately exposes tool name and argument names, not argument values/full outputs; it exposes a bounded receipt summary and clearly warns that summaries/corrections may be sensitive and untrusted.
- **Item 12 steering boundary:** corrections are durable guidance for one exact pending/ready step. They may adjust argument values only within the step's pinned tool/schema and existing authorization; they cannot change the planned tool, grants, approvals, completed work, or receipt state. Steering is rejected while the task is executing or the receipt/step is ambiguous/terminal. Corrections reuse append-only task events; the audit-retention/purge policy remains unresolved.
- **Item 10 status:** in progress. Owner-scoped expiry/sensitivity/dedupe and semantic lesson lookup now exist in `delilah_memories`; verified-success reflection stores fixed, receipt-bound procedural text. The origin-turn resolver foundation is pushed. Next: add exact-quote source resolution for the user message and successful, receipt-matched `research_topic` excerpts; let the model select only exact quotes, enforce user-only decisions, and persist the quote with typed provenance/evidence/TTL. Do not treat a paraphrase or search-result embedding as proof, do not bypass receipts, and do not write a FACT/DECISION until source validation and owner-scope tests pass. World Model `valid_to` remains validity history, not a memory TTL.
- **Database safety incident requiring owner review:** an earlier test/import was mistakenly run from the repository working directory, so `data/finances.db` was touched before later tests were moved to temporary directories. Known additive schema changes: `kg_claims` received `owner_user_id`, `visibility`, and an index; `user_preferences` received the operating-profile columns. The dossier-scope migration may or may not have run against that file; this was not rechecked. One synthetic confirmed `manage_user_profile` receipt exists for turn `profile-turn` (receipt ID `receipt_a09c04c521244435b66eb8726fcef022`). Four synthetic pending `kg_predictions` for `fws_cap_v1` / `org:cuny_hpc` / `fws_cap_date` were inserted, with latest observed timestamps around `2026-09-26 08:22–08:32 UTC` and rowids 218–221; the preexisting row count was not established. No Plaid/Gmail or other external account operation was performed. These rows were deliberately left untouched; do not delete or alter them without the owner's direction. All verification after the incident used temporary databases.
- **Review first:** owner should inspect the live-database safety incident above, then review Item 10's exact-quote FACT/DECISION source-binding design and Item 14's transition from fake contract to an isolated real worker manager. The current WIP commits do not activate runtime environments or persist FACT/DECISION memories.

## Progress

### Item 13 — user-scoped operating profile and inferred preference controls — passed

- Added owner-scoped typed profile inspect/set/forget with per-field user-stated source-turn provenance. Exact current-turn contracts are required for edits; profile values cannot grant tools or bypass ordinary authorization. `require_mutation_confirmation` only adds a stricter current-turn check. `preferred_model` and notification settings are explicitly record-only.
- Inferred preference learning requires both the operator switch and the authenticated user's explicit profile opt-in. Inline, background, direct World Model, and legacy save-memory preference paths enforce that policy; claim persistence rechecks the user opt-in inside a `BEGIN IMMEDIATE` transaction so opt-out cannot race past the write. Model-inferred candidates remain `INFERRED`/low-authority and are labeled as untrusted, not user-confirmed facts or instructions. Delegated children cannot change the owner-global profile.
- Added owner/visibility fields and fail-closed legacy quarantine for claims and dossiers; scoped FTS, semantic context/search, direct entity/dossier reads, `!inspectmemory`, full vector indexing, and vector reconciliation against SQLite ownership/current-validity. Hostile/stale vector payloads cannot override current SQLite content. Unscoped private user-anchor lookup is denied.
- Verification: Item 13 focused suite: 73 passed; `py_compile` and `git diff --check` passed. Full isolated suite: 1,168 passed, 1 skipped, 9 seed/fixture failures and 15 legacy World Model DB-lock errors described above. Fresh adversarial review: no blockers. Reviewer suggestions were addressed by rejecting unscoped user-anchor entity lookup and indicating the `!inspectmemory` 500-claim cap.
- Gate commit: `[gate] Item 13 user-scoped profile and memory privacy; 73 focused tests pass` (`95f22d4`), pushed to `agentic-roadmap/full-run`.

### Item 15 — safe capability proposal drafts — passed

- Added a bounded, standard-library-only validator for data-only adapter/workflow proposals. It rejects executable/code payloads, unknown or control tools, existing-name collisions, malformed/deep/oversized schemas, mismatched permissions, and workflow steps not covered by the requested permission list. Drafts carry a stable digest and are always `status=draft`, `available=false`, `runnable=false`.
- Added the model-facing `draft_capability_proposal` control tool. It is eligible only when the exact current user request appeared earlier in the same conversation, a successful unrestricted semantic discovery found no tool or workflow, and no later discovery attempt is inconclusive. Failed/invalid search attempts, restricted scope, matches, exploration, or schema-load attempts latch the turn against proposal creation. This state is turn-local; there is no parallel proposal ledger.
- Proposal output is explicitly untrusted and operator-review-only. Nothing is installed, registered, approved, activated, granted, or executed; the call itself uses the ordinary tool grant/receipt boundary. No capability execution or financial side effect is retried.
- Verification: focused proposal/catalog tests: 58 passed; full isolated suite: 1,215 passed, 1 skipped, 9 clean-seed/fixture failures, and 15 legacy World Model DB-lock errors. `py_compile` and `git diff --check` passed. Fresh adversarial review: no blockers.
- Deferred suggestions: add a full dispatcher integration test for malformed arguments and pre-dispatch scope denial after an earlier eligible miss; keep the independent workflow-registry control-name set synchronized (runtime parity is asserted, but the two definitions are not yet consolidated); proposal drafts are intentionally not persisted/deduplicated.
- Gate commit: `[gate] Item 15 draft-only capability proposals; 58 focused tests pass` (`1099ee6`), pushed to `agentic-roadmap/full-run`.

### Item 10 — Plan–execute–reflect–memorize — in progress

- Added `expires_at`, `sensitivity`, and owner-scoped `dedupe_key` support to epistemic memory. Semantic lookup now excludes expired records, can filter by memory type, and exposes sensitivity. Local/auto embedding paths do not silently fall back to cloud; a cloud embedding route is used only when explicitly configured. `.env.example` documents `REFLECTION_MEMORY_TTL_DAYS=0` as no expiry and a positive owner-selected TTL.
- Added a post-delivery reflector that runs only after persisted task success and `task.verification_passed`. It sends no objective, user message, step prose, arguments, results, or raw persisted IDs to the model. The model gets generated choice labels plus canonical registered tool names and can select a fixed pattern. The saved lesson is deterministic text derived from the selected tool and a task step receipt linked by the successful verification event. It cannot create facts, decisions, permissions, or model-authored lesson text. Lessons are searchable as untrusted advisory context only.
- Added `origin_turn_id` to new top-level `task.created` events and an owner/session-scoped source-message lookup; it reads only `role='user'` messages from that exact turn, has no task-ID parsing or search fallback, and preserves any credential redaction already applied at message persistence. This is groundwork only; FACT/DECISION extraction and web-source validation are not yet wired.
- Verification: focused memory/reflection/task-environment/source-provenance/controller suite: 108 passed. Latest full isolated suite: 1,295 passed, 1 skipped, 9 known clean-seed/fixture failures, 15 legacy World Model DB-lock errors. Fresh adversarial critic: no blockers. Reviewer suggestions still deferred: LLM outbound-request/provider integration test; prevent redundant concurrent reflection requests with an atomic claim. Configured model-candidate retries at the chosen provider endpoint remain governed by that provider configuration; no cross-provider reflection fallback is added.
- WIP commits, all pushed: `2e31ff0 [wip] Add verified task lesson reflection with owner TTL`; `8ef6874 [wip] Bind reflection sources to exact origin turn`. No Item 10 gate claimed. Next work is exact-quote user/web FACTs and user-only DECISIONs after server-side source validation, including receipt/call/turn/source binding for web research.

### Item 14 — secure task environments and capability leases — partial/deferred

- Source audit confirmed that current Browserless/rendered-page services are shared rendering, not task-owned interactive browser workers. The existing Python/shell sandbox is a shared service with persistent workspace, internal-peer plus public network access, request-supplied identity/shared token, and a very large output cap applied after collection; the bot container runs as root and mounts finance data. These are not a task VM boundary. No on-demand worker, task-scoped browser profile/network policy, encrypted continuity lifecycle, or teardown path was invented without the interactive-browser feature and a deployment threat model.
- Hardened the existing Browserless diagnostic host port: default bind is loopback (`127.0.0.1:3002`), with an explicit operator override; Delilah continues using the internal service URL. Compose configuration passed for both the default and an explicit public-bind override. Updated the Compose exposure summary to avoid claiming only four services publish host ports.
- Removed bot Docker socket, Docker CLI packages, and runtime `docker inspect`/hardcoded-IP Stirling fallback. Stirling now uses configured URL/Compose service DNS. Corrected the misleading sandbox-network comment. Deep research is in `docs/muse-secure-environment-research.md`. `docker compose config --quiet` and focused forms tests passed; no services were started, so DNS/reachability are not claimed.
- Added the initial host-owned `TaskEnvironmentManager` contract, fixed no-network/no-credential resource profiles, allowlisted operations, task+owner lease checks, timeout/cancel/cleanup/recovery paths, and a fake adapter. Its output API streams bounded chunks and the test uses a lazy source to prove the manager stops advancing at the cap. Fresh adversarial critic: no blockers. This remains a fake contract only; there is no process/container launch or runtime authority, and the real adapter must independently enforce streaming limits and receive authenticated host identity.
- Remaining before interactive browser automation: task/user-bound worker manager; CPU/memory/runtime/output bounds; deny-by-default egress and SSRF controls; executor-only secrets; encrypted state only when requested; guaranteed teardown; host-side finance authorization/receipts. Optional microVM is an operator-selectable stronger backend, not an always-on prerequisite. Partial commits: `dfc08b3`, `f45feef`; no Item 14 gate claimed.
- Contract WIP commit: `ae29641 [wip] Define task-owned environment contract and fake adapter`, pushed. Reviewer suggestions deferred: exercise cleanup-adapter exceptions before a real backend; keep guest/model-facing code unable to construct or supply `TaskIdentity`; add real-backend streaming tests. No runtime services were started.

### Item 12 — semantic traces and mixed-initiative steering — passed

- Added bounded `inspect_task(task_id, detail_level)` for tasks visible only in the current owner/conversation scope. Stable step IDs/status, descriptions, criteria, tool/receipt references, and explanations are available in summary mode; trace mode adds tool names/argument names and an owner-matched, bounded receipt summary, never raw arguments or full results. Persisted task text, summaries, and corrections are marked untrusted; trace output warns that content may be sensitive.
- Added model-facing `steer_task(task_id, step_id, correction)`. Exact explicit user correction is required by the autonomy contract. It appends a task event and preserves completed work; the next inference receives an atomic version/guidance snapshot. The correction may affect argument values only within the pinned step tool/schema and cannot alter grants, approval, completed work, or receipt state. No side effect is retried.
- Closed both stale-generation windows: task guidance and steering-event revision are read atomically for each inference; a task update during generation discards tool calls. If steering lands after generation's version check, `executing` phase blocks later steering, while the assistant tool-call block transaction compares the captured steering event ID and rejects stale output before persistence or Gmail prefetch. That path restores planning and re-infers with current guidance. Tests assert no stale call row, receipt, grant, Gmail prefetch, or handler execution.
- Verification: focused suite (`test_task_steering_store.py`, tools, dispatch, autonomy contract, task verification, catalog, workflow registry): 223 passed; full isolated suite: 1116 passed, 1 skipped, 10 known clean-seed/fixture failures listed above. `py_compile` and `git diff --check` passed. Fresh adversarial critic: no blockers.
- Deferred suggestions: add an actual simultaneous multi-connection phase-transition/steering stress test; consider stronger redaction for owner-visible receipt summaries/corrections. The trace intentionally reports argument names, not values/full results.
- Gate commit: `[gate] Item 12 semantic traces and race-safe steering; 223 focused tests pass` (`1caacd6`), pushed to `agentic-roadmap/full-run`.

### Item 8 — reusable versioned declarative workflows — passed

- Added an immutable, data-only workflow registry with bounded trigger hints, required canonical tools, ordered steps, guardrails, closed JSON argument/result schemas, exact numeric versions, and a stable digest. The initial built-in set covers Gmail triage and monitor creation/readback; discovery exposes candidates only and never grants tool permission.
- Workflow selection requires the exact candidate ID/version/digest. The server derives the steps, and `SessionStore` atomically persists the task plan and version/digest pin in the existing plan event. Task runs remain the only progress ledger; facts remain in the World Model. If the exact pinned version is unavailable or altered, dispatch fails closed.
- Workflow steps reject all plan-control tools. The controller blocks advancing while a step is running, and the transactional store independently rejects a second concurrent step claim. Every workflow action still follows existing dispatch-time allowlist, authorization/grant, and receipt checks; ambiguous side effects are not retried.
- Verification: focused registry/catalog/discovery/persistence suite: 81 passed; full isolated suite: 1070 passed, 1 skipped, 10 known clean-seed/fixture failures. `py_compile` and `git diff --check` passed. Fresh adversarial review: no blockers.
- Deferred suggestions: add a full dispatcher-level test proving invalid workflow arguments/out-of-order calls stop before grant, receipt, and handler invocation; explicitly label workflow text as untrusted if external authoring is ever introduced. The current registry is built-in-only and performs validation before the normal dispatch boundary.
- Gate commit: `[gate] Item 8 versioned declarative workflow registry; 81 focused tests pass` (`2bc7c09`), pushed to `agentic-roadmap/full-run`.

### Item 6 — explicit autonomy contracts for vague requests — passed

- Added a frozen typed per-turn contract with mode, objective, permitted initiative, required evidence, completion criteria, explicit mutation scope, required tools, and a user-visible selected-scope status summary. Contracts use only the current redacted user message plus narrowly scoped prior user context; quoted/explanatory requests do not trigger a financial refresh.
- Open-ended financial reviews enforce the ordered pull-only workflow `sync_plaid_accounting(force_refresh=true, reconcile=false, post_summary=false)` → current position → debt → upcoming bills. Dispatch rejects out-of-scope tools, extra mutations, reordered/repeated workflow actions, and premature completion; Gmail prefetch is disabled in this strict workflow. Required evidence failures and ambiguous outcomes stop rather than retry.
- Added explicit, exact-scope handling for user-requested task cancellation (a named task ID must match; an unnamed request requires exactly one active task in the owner/conversation scope). Ambiguous/partial cancellation is reported as possibly effective and needing reconciliation. Explicit monitor creation is permitted only through `monitor_create_natural_rule` with the exact user-requested instruction; ordinary monitor inspection stays read-only. Delegation remains a bounded internal control for ordinary execution/investigation/planning/continuation modes, and is denied in monitor, await-decision, and strict financial-review modes.
- Added a final contract-response guard covering loop breakers and provider fallbacks: a direct unmapped mutation is deterministically reported as not performed, and no final success is accepted when contract-required tools lack successful evidence. Alias effect classification is normalized; unknown tools fail closed. Permitted calls still use the existing grant and receipt lifecycle.
- Verification: focused suite (`test_autonomy_effect_catalog.py`, autonomy contract, tool intent, task verification, task controller): 234 passed; `py_compile` and `git diff --check` passed. Full suite in a temporary working directory with credentials unset and Plaid autosync disabled: 1016 passed, 1 skipped, 10 existing seed/fixture failures (detailed in the current stopping summary). The repository finance database was not used. Fresh adversarial review: no blockers.
- Deferred suggestions are listed in the current stopping summary above. No semantic/model reviewer was added to intent classification; the contract is deterministic and dispatch remains authoritative.
- Gate commit: `[gate] Item 6 explicit autonomy contracts; 234 focused tests pass` (`a81eea4`), pushed to `agentic-roadmap/full-run`.

### Item 5 — semantic tool discovery with authoritative authorization — passed

- Added model-callable `search_tools(query, task_context)` on ordinary turns. `ToolRegistry.search_tools` owns retrieval invocation and result shaping, reusing the hybrid/lexical router and canonical `BOT_TOOLS_SCHEMA` rather than creating a second index or tool catalog.
- Results are capped at six schemas and 32,000 schema characters, preserve retrieval order, and are filtered against the current discoverable scope. Delegation uses `CURRENT_ALLOWED_TOOLS`; input-only continuations use their read-only allowlist; audit/fresh-verification/Gmail-only modes retain their fixed schemas. Retrieved names enter only the next model schema round; dispatch-time controller, grant, argument, receipt, and side-effect checks remain authoritative.
- Availability is `null` unless an operational probe exists; reasons explicitly say runtime dependencies and credentials are not checked. Unknown side effects normalize to `unknown`; schema examples are returned only when declared. Retrieval failure returns an unavailable result while preserving the existing domain/schema-load fallback.
- Verification: focused discovery/registry/router/catalog/intent/controller suite: 107 passed; full isolated suite: 871 passed, 1 skipped, 10 known clean-seed failures. `py_compile` and `git diff --check` passed. Fresh adversarial review: no blockers; the suggested label-normalization cases were added and tested.
- Deferred suggestion: add an end-to-end mocked provider-loop test proving a discovered mutation appears in the next round yet still fails without the usual grant and has receipt evidence. Runtime dependency availability probes and source-declared examples remain absent; metadata fails closed rather than guessing.
- Gate commit: `[gate] Item 5 registry-owned semantic tool discovery; 107 focused tests pass` (`d260fee`), pushed to `agentic-roadmap/full-run`.

### Item 11 — deterministic verification and repair gates — passed

- Added a pure bounded verifier for linked receipts, per-source freshness, exact-decimal arithmetic, allowed-tool policy, required deliverables, financial numeric claims, and file identity/format evidence. Failed checks atomically transition a verifying task to partial, append a bounded task event, and create one ready manual repair step; ambiguous effects are never retried.
- Every non-control tool call in a durable task now links to a task step and receipt before dispatch. A missing durable task context stops the advisor before model/tool execution. An unknown receipt stops both the current tool batch and subsequent provider-split batches/rounds.
- Typed current-position snapshots provide the only current financial freshness/arithmetic proof today; unsupported financial sources and untyped reconciliations fail closed. JSON exports are validated from exact delivered bytes with duplicate keys and nonstandard constants rejected; CSV exports require bounded UTF-8 parsing, consistent row widths, and reject JSON documents/scalars mislabeled as CSV. Missing canonical sandbox paths fail before sending.
- Added an optional `DELILAH_TASK_MODEL_REVIEWER` pass (default off) for multi-step tasks after deterministic checks pass. It receives bounded objective/response/check summaries, runs with tools disabled, and cannot replace deterministic checks. Invalid/unavailable review fails closed.
- Verification: focused Item 11 suite: 135 passed; latest full isolated suite: 853 passed, 1 skipped, 10 known clean-seed failures. `py_compile` and `git diff --check` passed. Fresh adversarial review: no blockers.
- Deferred suggestion: add a full mocked provider-loop integration test proving no later call executes after an unknown receipt. Other deferred constraints and the clean-database failures are listed in the current stopping summary above.
- Gate commit: `[gate] Item 11 deterministic verification and repair gates` (`ae9fd15`), pushed to `agentic-roadmap/full-run`.

### Item 3 — durable background execution and completion delivery — passed for single-process deployments

- Added startup recovery and a recurring fair queue sweep for durable delegated child tasks, reusing `task_runs`/`task_steps` and existing call/receipt evidence. Added per-owner queued and active caps, configurable worker count, provider limits, pagination, and an OS-held database process lock; another bot process using the same local DB path fails closed.
- Added restart-safe child completion notices with owner-scoped destination lookup, status-only content, atomic claim/start/delivered events, and no resend after a potentially ambiguous send. Invalid scopes, resume failures, and startup recovery failures atomically park the exact child/parent pair for reconciliation. Periodic recovery does not reinterpret live running tasks as stale.
- Deployment boundary: multiple worker coroutines inside one Delilah process are supported; simultaneous processes/hosts sharing the DB/provider are not. This topology decision remains open; no TTL or fencing policy was invented.
- Verification: final full isolated suite: 721 passed, 1 skipped, and 10 known clean-database/seed-data failures. `py_compile` and `git diff --check` passed. Fresh critic found no blockers in startup recovery/pagination fixes.
- Deferred suggestion: add an integration test showing direct delegation and startup recovery contending for the same owner's active-child cap.

### Step 4 — bounded delegation as child tasks — passed

- Wired model-facing `delegate_task` and conversation-scoped `task_cancel`; parents and children are inspectable through `task_list`. Child tasks have isolated sessions, durable parent links, bounded budgets, a parent-issued capability grant, and no recursive delegation.
- Parent and child states are durably linked to ordinary `tool_calls` and `tool_receipts`. Every delegated action creates a task step. Recovery validates the parent delegate call, receipt turn/hash, child goal, tool scope, and budget before resuming; missing/mismatched evidence parks the task for reconciliation.
- Safe queued children are rehydrated on the next matching owner/session/channel/thread interaction. A CAS claim prevents duplicate resume. Recovery derives consumed steps from existing call rows, honors the original elapsed timeout, and never retries `started`/`unknown` action receipts. The parent remains `awaiting_child` until a known terminal result.
- Scheduler child slots preserve one-worker sequential behavior and cap child execution by both configured workers and provider capacity. Tests exercise one and multiple workers with provider ceilings of one and three.
- Verification: 114 focused tests passed; full isolated suite: 676 passed, 1 skipped, 10 known clean-database/seed-data failures; `py_compile` and `git diff --check` passed. Earlier adversarial blockers were fixed and re-reviewed; the final fresh review found no blockers or suggestions.
- Deferred to Item 3: run queued-child recovery from scheduler/process startup without waiting for a new interaction, and provide the general completion-delivery path.
- Gate commit: `[gate] Step 4 bounded delegation; 114 focused tests pass` (`93cd834`).

### Step 6 — tune limits and expand from observed use — passed

- Added bot-owner-only `!schedulerstatus`/`!scheduler` DM diagnostics with rolling bounded p50/p95/max for queue admission (includes scheduler/provider wait), scheduler provider-capacity wait, scheduled inference work (excludes scheduler slot wait but includes the scheduled callable), and tool dispatch/receipt duration; included current queue, provider limits, active workers, cancellations, and dispatch counts. Tenant/task identifiers are omitted.
- `.env.example` documents conservative defaults and provider limiter-key mapping. Known Ollama background inference call sites declare `provider="ollama"`; main model, wakeup, claim extraction, and history summary paths reject unsupported `LLM_PROVIDER`/`WAKEUP_PROVIDER` values before scheduling or issuing a request.
- Verification: scheduler/forms/provider-config focused suite: 63 passed. Full isolated suite: 1,230 passed, 1 skipped, 9 clean-seed/fixture failures, and 15 legacy World Model database-lock errors. `py_compile`, `git diff --check`, and Compose config passed. Fresh critic: no blockers. No workload data justified changing one-worker/one-active-task-per-owner/eight-queued-per-lane defaults; no empirical tuning was performed. Owner selected one bot process per task DB; no distributed leases are implemented.
- Deferred suggestions: add provider-round latency separate from scheduled-callable duration; test unsupported provider settings across main/wakeup paths; test diagnostics redaction with recognizable queued IDs; add a multi-worker authorized-receipt integration test. Gate commit: `[gate] Step 6 scheduler diagnostics and provider-aware capacity` (`fef9921`), pushed.

### Step 3 — scheduler and capacity controller — passed

- Built `CapacityAwareScheduler` and `ProviderLimiter` (`src/agent/scheduler.py`) enforcing hard concurrency ceilings at the provider client boundary across Ollama, OpenAI, OpenRouter, and Kev.
- Implemented fair round-robin owner rotation with dynamic arrival fairness, longest-wait lane selection between interactive and background lanes, interactive tie-breaking, and per-lane queue capacity limits preventing background backlogs from starving or crashing interactive turns.
- Enforced non-preemptive cooperative cancellation at dispatch boundaries without leaking worker slots or task counters.
- Propagated context variables (`CURRENT_TASK_ID`, `CURRENT_TURN_ID`, session tokens) across asynchronous worker tasks via `asyncio.create_task(..., context=unit.context)`.
- Routed all known model work through the scheduler at unit granularity:
  - Interactive stream inference (`stream_generator`) via `schedule_work(..., "interactive", unit_type="inference", provider=llm_provider)`.
  - Background classification (`classify_with_local_llm` and `classify_transaction_batch`).
  - Autonomous analyst (`run_autonomous_analyst`).
  - History summarization (`_summarize_history_block`).
  - Claim extraction (`auto_extract_and_persist_claims`).
  - Kev decision provider (`DecisionProvider.decide`).
  - Ollama warm/unload/preload operations in `commands.py`.
  - Batch and singular local Ollama embedding in `qdrant_client.py` gated with `provider_capacity("ollama")`.
- Real-time observability: recorded queue wait, inference duration, tool duration, cancellations, and dispatch distributions in `SchedulerMetrics`.
- Verification: 23 dedicated scheduler tests in `tests/test_scheduler.py` covering hard concurrency, owner rotation on dynamic arrival, continuous readiness arbitration, re-entrancy, waiter leak protection, cooperative cancellation, queue lane isolation, nested inference/decision turns, and embedding limiter gating. Full test suite passes: 657 passed, 1 skipped. Adversarial critic review approved with 0 blocking items.
- Deferred suggestions: wrap cloud embedding branches in `qdrant_client` with `provider_capacity(backend)`, assign explicit owner ID to tool/classifier work when triggered during an interactive turn, and persist scheduler metrics in Step 6.
- Gate commit: `[gate] Step 3 route model work through capacity-aware scheduler` (commit `c0771ec`).


### Item 1 — durable task plans and inspection — passed

- Added model-facing `task_plan` and read-only `task_list`. Plans persist ordered tool actions, descriptions, observable completion criteria, retry count, status, and next action on existing `task_steps` rows; no second execution ledger was introduced. Migration 009 adds the fields idempotently.
- Enforced plan-first behavior for detected three-or-more-action requests, artifacts, and side-effecting tools. Whole batches are rejected before dispatch if a plan is missing; `fetch_webpage(save_only=true)` is included. Planned steps must dispatch the exact next tool, link the existing tool-call and matching receipt records, and stop after a failed or ambiguous result. A plan is not authorization or approval. The policy classifier is deterministic and conservative, not a semantic guarantee for every possible paraphrase.
- `task_list` is owner/session/channel/thread scoped and bounded in both SQL and output; persisted descriptions are treated as untrusted text and task status is not evidence that an external effect succeeded.
- Verification: focused task/controller/store/catalog/recall suite: 50 passed; full suite: 600 passed, 1 skipped, 10 known clean-database/seed-data failures. `py_compile` and `git diff --check` passed. Fresh adversarial critic: no blockers.
- Deferred suggestions: add a dispatcher-level `task_list` scope/read-only test and a model-usable lookup from steps to call/receipt evidence. `retry_count` is persisted but stays zero until a separately specified safe-retry policy exists. Plan revisions are not supported. Action-bound approval for resumed side effects remains a Step 2 dependency; plans do not grant consent.

### Item 4 — durable session recall — passed

- Added bounded `search_session_history` model tool plus automatic recall cues backed by `SessionStore.search_messages`; recalled records include session/turn IDs, timestamps, freshness, and explicit untrusted-content framing. Prompt cache is scoped to exact owner/session/channel/thread and bounded to six messages/6,000 characters; returned history is bounded to ten messages/1,600 characters each.
- Gmail/mail-only turns and turns that actually invoked Gmail tools are excluded from future recall. Legacy turns are also excluded using owner-scoped durable Gmail tool-call evidence. An unlinked legacy Gmail call fails closed for that conversation because its associated turn cannot be identified. The lookup is bounded by retained messages rather than only the newest tool-call rows.
- Verification: focused session recall/store/tool catalog suite: 25 passed. Full suite in an isolated temporary working directory: 590 passed, 1 skipped, 10 failed. The same clean-database/seed-data expectation failures and pre-existing Discord budget-dashboard failure remain. `py_compile` and `git diff --check` passed. Fresh final critic: no blockers.
- Deferred critic suggestions: test Gmail exclusion through the full dispatcher rather than the marker helper; add a model-level behavioral prompt-injection evaluation; test many intervening non-Gmail calls with an older retained Gmail call; document/test that a stale unlinked Gmail record can suppress recall for the entire conversation until pruning. Intent classification for mixed mail/financial prompts remains heuristic and merits separate policy review.

### Item 9 — first-class await and resume — passed

- Added the model-facing `await_user` tool. It requires an active durable task, validates a 1–1000 character question and 2–8 distinct exact choices, persists an expiring question (5 minutes–7 days; default 24 hours), displays a task/question resume token, and terminates the current turn after the question. Mixed tool batches containing `await_user` are rejected before execution.
- Exact unique choice replies are routed to the same owner/session task. Replies arriving while the original advisor turn is finishing retain the resume match in the queue and are resumed after it drains. Expired questions are atomically cancelled; unrelated or ambiguous text remains a normal new turn. Resume prompts carry the objective and persisted step/action states.
- User answers are marked input-only, never approval. During that continuation, dispatch permits only an explicit read-only allowlist and denies unclassified tools, writes, file operations, and `fetch_webpage(save_only=true)` before dispatch.
- Verification: focused task/controller/catalog/recall/bot suite: 39 passed. Full suite in an isolated temporary working directory: 594 passed, 1 skipped, 10 known clean-database/seed-data failures. Fresh critic: no blockers.
- Deferred critic suggestions: add a dispatcher-level test proving blocked calls never reach handlers; atomically claim an answer before multiple duplicates can queue; page beyond the 500 newest waiting tasks. The explicit `!resume` prompt now carries the persisted step identity/action state.

### Item 7 — durable phase-based recovery — passed

- Added migration 010 with durable task `phase` and `phase_next_action` fields, including legacy-state backfill. Phase changes use task-version CAS, append-only `task_events`, and bounded conflict retry only when the phase itself did not change.
- Wired received/planning/executing/awaiting-user/verifying/delivering/completed/partial/failed phase updates into the runtime. Final Discord delivery exceptions are treated as ambiguous: no fallback resend/edit is attempted; task status becomes partial and restart recovery repairs status/phase split writes.
- Provider assistant tool-call batches now persist ordered IDs, tool names, and argument rows plus their manifest event atomically before dispatch. Added a bounded reconstruction API; ordinary retention preserves referenced turns and call rows.
- Restart recovery checks every unlinked persisted block before any task-step recovery branch, checks linked receipts against exact call/owner/tool/argument hash and rejects duplicate receipts, resumes only calls proven not started, and parks ambiguous or inconsistent evidence for manual reconciliation. Recovery repairs execution, delivery, terminal-status, and phase split windows.
- Verification: focused task-controller/session-store suite: 52 passed. Latest full isolated suite: 623 passed, 1 skipped, 10 known clean-database/seed-data failures. `py_compile` and `git diff --check` passed. Fresh adversarial critic: no blockers.
- Deferred critic suggestions: add an integration test proving phase/tool-call persistence precedes tool invocation; consolidate controller ownership during a later runtime extraction. `awaiting_child` is a supported phase but is not entered until Item 2 implements child execution.
- Deferred safety/product decisions: exact results from completed unlinked tool blocks are not automatically injected/replayed (fail closed and require recovery rather than risk a duplicate); retention/privacy purge policy remains an operator-level decision. Step 2 approval/grant and notice-delivery decisions remain unresolved.

### Step 2 — resumable task execution — passed

- Added `TaskController.recover_incomplete` to project linked receipt outcomes after process restart across all phases (`executing`, `planning`, `verifying`, `delivering`). Preserves `restart_after_confirmed_failure` when steps have failed, retains `monitor_readback_required`, preserves `plan_pending` wait reasons, and recovers unstarted calls to `restart_before_dispatch` with stale prepared receipt cleanup.
- Added `TaskController.validate_tool_dispatch` enforcing `TASK_NOT_DISPATCHABLE`, `MONITOR_READBACK_REQUIRED`, and `TASK_STEP_ALREADY_CONFIRMED` replay prevention, with readback exemption for `monitor_list_rules`. Delegated `llm.py` tool dispatch validation to `TaskController.validate_tool_dispatch`.
- Resumption protocol: exact choice answers via `accept_reply` resume tasks into planning phase with `reply_is_input_only=True`, denying write/mutation tools unless separately authorized.
- Proactive reconciliation surfacing: `surface_reconciliation_once` backed by migration 008's unique index `ux_task_reconciliation_notice_claimed_once` guarantees at-most-once notice delivery without race conditions.
- Verification: `tests/test_task_controller.py::test_step2_resumable_execution_full_gate` verifies read-only workflow restart without work replay, monitor insert -> restart -> readback workflow, pending-step crash recovery, unknown outcome reconciliation and replay blocking, choice reply matching/rejection, confirmed failure preservation, and plan_pending preservation across repeated restarts. All 41 task controller tests pass; full test suite passes (634 passed, 1 skipped). Adversarial critic review approved with 0 blocking items.
- Deferred suggestions: prune legacy pre-phase recovery branches in `recover_incomplete`.
- Gate commit: `[gate] Step 2 resumable task execution and workflow recovery` (commit `6e34f70`).

### Step 0 — workflow action contracts — passed

- Documented owner/effect, authorization, retry, evidence, and reconciliation contracts for Gmail search/read and monitor create/list.
- Added fail-closed receipt admission for both advisor monitor-insert tools: an owner’s unresolved `started`/`unknown` monitor receipt blocks either insert tool. Ambiguous insert/commit failures remain `unknown`; receipt finalization reads back terminal evidence after a lost commit acknowledgement.
- Verification: targeted contract/receipt/monitor tests: 32 passed. Full suite in an isolated temporary working directory: 560 passed, 1 skipped, 9 failed. The failures are legacy finance/world-model tests expecting seeded records absent from the clean test DB; the repository’s real `data/finances.db` was not used or mutated. `git diff --check` clean.
- Fresh adversarial review: no blockers. Deferred suggestions: add dispatcher-level tests for the whole model-dispatch path and exact serialized config output; route Discord `!monitor create` / `!monitor add` commands through the guarded receipt lifecycle. Those command paths are explicitly outside the Step 0 advisor-tool workflow and should be revisited before expanding monitor task recovery.
- Follow-up requirement: the current dispatcher auto-issues its internal mutation grant; it does not independently bind that grant to explicit user intent or human approval. Step 2 must enforce task/action intent binding before resumable monitor dispatch.

### Step 1 — durable task, step, and event records — passed

- Added migration 007 and owner-scoped `SessionStore` APIs for task runs, ordered/dependent steps, versioned CAS transitions, and append-only events. Steps reference existing tool-call IDs and owner/call-matched receipt IDs; queue ordering stays on task rows.
- Event payloads are restricted to task metadata/evidence references. Referenced tool calls and their turns are retained past the ordinary per-session cap; task/event deletion is restricted until an explicit purge policy exists.
- Verification: focused session-store tests: 8 passed. Full suite in isolated temporary working directory: 564 passed, 1 skipped, 9 failures from legacy finance/world-model tests requiring seed records absent from the clean DB. The live finance DB was not used or mutated. `git diff --check` clean.
- Fresh adversarial review: no blockers. Deferred note: `receipt_id` has no SQL foreign key because `ReceiptStore` installs its table independently; `SessionStore` verifies receipt ownership and matching call ID on every link/update. No receipt-deletion API currently exists.
